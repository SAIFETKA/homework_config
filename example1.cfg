var host := "localhost";
var port := 8080;
var endpoints := #("login" "logout" "status");
var server_config := {
    max_connections = 100,
    timeout = 30.5
};
var total_clients := ?(+ server_config.max_connections 10);
