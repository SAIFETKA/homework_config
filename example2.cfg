:: Конфигурация веб-сервера
var server_name := "my-web-server";
var listen_port := 8080;
var ssl_enabled := true;

:: Настройки базы данных
var database := {
    host = "localhost",
    port = 5432,
    name = "myapp",
    user = "admin"
};

:: Пути API
var api_endpoints := #("/api/v1/users" "/api/v1/posts" "/api/v1/comments");

:: Количество воркеров
var worker_processes := ?(* 2 ?(+ database.port 100));